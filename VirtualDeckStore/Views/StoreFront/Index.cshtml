@model VirtualDeckStore.Models.Category

@{
    ViewBag.Title = @Model.Name;
}


<br/>
<br/>

<div class="row">
    <div class="col-xs-12 col-md-3">
        <ul class="nav nav-pills nav-stacked">
            @if(Model.Name=="Adventure")
            {
                <li class="active">
                    <a href="~/StoreFront/Index/1">Adventure</a>
                </li>
            }
            else
            {
                <li>
                     <a href="~/StoreFront/Index/1">Adventure</a>
                </li>
            }
            @if (Model.Name == "Racing")
            {
                <li class="active">
                    <a href="~/StoreFront/Index/2">Racing</a>
                </li>
            }
            else
            {
                <li>
                    <a href="~/StoreFront/Index/2">Racing</a>
                </li>
            }
            @if (Model.Name == "RolePlaying")
            {
                <li class="active">
                    <a href="~/StoreFront/Index/3">Role Playing</a>
                </li>
            }
            else
            {
                <li>
                    <a href="~/StoreFront/Index/3">Role Playing</a>
                </li>
            }
            @if (Model.Name == "Strategy")
            {
                <li class="active">
                    <a href="~/StoreFront/Index/4">Strategy</a>
                </li>
            }
            else
            {
                <li>
                    <a href="~/StoreFront/Index/4">Strategy</a>
                </li>
            }
            
        </ul>
    </div>
    <div class="col-xs-12 col-md-9">
        <div class="row">
            @foreach (var product in Model.Products)
            {
                string prodName = product.Name;
                prodName = prodName.Replace(" ", String.Empty);
                <div class="col-xs-12 col-md-3">
                    <div class="thumbnail">
                        <img class="img-responsive" src="~/Content/Images/Products/@(prodName).jpg">
                        <div class="caption text-center">
                            <p>
                                <b>Name:</b> <span>@product.Name</span>
                            </p>
                            <p>
                                <b>Price:</b> <span>@product.Price USD</span>
                            </p>
                            <p class="text-center">
                                @Html.ActionLink("Add to cart", "AddToCart", "ShoppingCart", new { id = product.Id }, new {@class="btn btn-info"})
                            </p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
